sumar usuario a grupo sudoers
  su - 
  # poner contraseña root

  usermod -aG sudo usuario

instalar sudo
  apt install sudo

reiniciar
  shutdown now

install flatpack package manager (ojo, no instalar gestor de paquetes snap)

  sudo apt install flatpak

permitir acceso por escritorio remoto en pantalla de login

  sudo apt install xrdp
